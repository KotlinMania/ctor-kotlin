# Port targets — `ctor-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `ctor` that codex-rs uses, and therefore the minimum subset that `ctor-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-39] (rank `227`, tier `direct`, status `To Do`)

**Cargo package name**: `ctor`
**Rust module name (in `use` statements)**: `ctor`

## Summary

- **1** distinct symbols imported by codex-rs
- **4** total `use` statements
- **3** codex source files affected
- **3** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `ctor::ctor`

- **4 `use` site(s)** across **3 codex member crate(s)**: `codex-core`, `codex-exec-server`, `core_test_support`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/tests/common/lib.rs:7` (`codex-core`) — `use ctor::ctor`
- `core/tests/common/lib.rs:7` (`core_test_support`) — `use ctor::ctor`
- `core/tests/suite/mod.rs:8` (`codex-core`) — `use ctor::ctor`
- `exec-server/tests/common/mod.rs:15` (`codex-exec-server`) — `use ctor::ctor`
